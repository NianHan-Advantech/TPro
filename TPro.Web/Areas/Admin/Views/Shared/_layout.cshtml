<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - TPro.Web</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/elementui.css" />
    <link rel="stylesheet" href="~/css/admin/adminsite.css" />
</head>
<body>
    <div id="app">
        <el-container>
            <el-header>

                <el-menu :default-active="activeIndex" class="el-menu-demo" mode="horizontal" @@select="handleSelect">
                    <span>
                        Custom
                    </span>
                    <span>
                        <i :class="isCollapse?'el-icon-s-unfold':'el-icon-s-fold'" @@click="isCollapse=!isCollapse"></i>
                    </span>
                </el-menu>
            </el-header>
            <el-container class="custom-container">
                <el-aside :width="isCollapse?'64px':'200px'" class="custom-aside">
                    <el-menu default-active=""
                             class="el-menu-vertical-demo"
                             :collapse="isCollapse"
                             @@select="handleTableSelect">
                        <div v-for="(one,i) in menutree" :key="i">
                            <template v-if="one.Children.length>0">
                                <el-submenu :index="i.toString()">
                                    <template slot="title">
                                        <img :src="'i.toString()==activeIndex'?one.ActiveIconPath:one.IconPath" />
                                        <span>{{one.MenuName}}</span>
                                    </template>
                                    <el-menu-item v-for="(two,j) in one.Children" :key="j" :index="i.toString()+'-'+j.toString()">
                                        <img :src="'(i.toString()+'-'+j.toString())==activeIndex'?two.ActiveIconPath:two.IconPath" />
                                        {{two.MenuName}}
                                    </el-menu-item>
                                </el-submenu>
                            </template>
                            <template v-else>
                                <el-menu-item :index="i.toString()">
                                    <img :src="'i.toString()==activeIndex'?one.ActiveIconPath:one.IconPath" />
                                    <template slot="title">
                                        {{one.MenuName}}
                                    </template>
                                </el-menu-item>
                            </template>
                        </div>
                    </el-menu>
                </el-aside>
                <el-main>@RenderBody()</el-main>
            </el-container>
            <el-footer>
                <div class="container">
                    &copy; 2022 - TPro.Web - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
                </div>
            </el-footer>
        </el-container>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="~/lib/axios.js"></script>
    <script src="~/lib/vue.js"></script>
    <script src="~/lib/elementui.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
    <script>
        var app = new Vue({
            el: '#app',
            mixins: [mixin],
            data() {
                return {
                    activeIndex: '1',
                    isCollapse: true,
                    testmsg: 'test',
                    menutree: []
                }
            },
            mounted() {
                this.getmenutree()
            },
            methods: {
                getmenutree() {
                    axios.get("/admin/home/GetMenuTree")
                        .then((res) => {
                            if (res.data.Code == 200) {
                                this.menutree = res.data.Data;
                            }
                        }).catch(err => console.error(err))
                        .finally()
                },
                handleSelect(key, keyPath) {
                    console.log(key, keyPath);
                },
                handleTableSelect(key, keyPath) {
                    this.activeIndex = key
                    var indexs = key.split('-')
                    var node = this.menutree
                    indexs.forEach((item, i) => {
                        if (i == 0) {
                            node = node[item]
                        } else {
                            node = node.Children[item]
                        }
                    })
                    if (node?.RoutePath == undefined) {
                        this.$message.warning("错误的地址")
                    } else {
                        window.location.href = node.RoutePath
                    }
                }
            }
        })
    </script>
</body>
</html>
